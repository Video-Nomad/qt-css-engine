"""Benchmark suite for qt_css_engine.

Run with:
    uv run python -m benchmarks
    uv run python -m benchmarks --help
    uv run python -m benchmarks --filter cold
    uv run python -m benchmarks --json
"""
